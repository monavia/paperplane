export const CONSTANTS = {
  // === Shutdown ===
  SHUTDOWN_GRACE_PERIOD_MS: 30000,
  SHUTDOWN_SAVE_TIMEOUT_MS: 10000,
  SHUTDOWN_DESTROY_TIMEOUT_MS: 5000,

  // === Queue ===
  QUEUE_LOCK_TIMEOUT_MS: 30000,
  QUEUE_PAGE_SIZE: 10,
  BUTTON_COLLECTOR_TIMEOUT_MS: 30000,
  BUTTON_CONFIRM_TIMEOUT_MS: 15000,
  QUEUE_COLLECTOR_TIMEOUT_MS: 60000,
  SEARCH_COLLECTOR_TIMEOUT_MS: 60000,

  // === Voice ===
  ALONE_TIMEOUT_MS: 60000,
  DISCONNECT_TIMER_MS: 60000,
  MANUAL_ADVANCE_WINDOW_MS: 5000,

  // === Lavalink ===
  LAVALINK_HEARTBEAT_INTERVAL_MS: 30000,
  LAVALINK_RETRY_DELAY_MS: 10000,
  LAVALINK_RETRY_AMOUNT: 5,
  LAVALINK_REQUEST_TIMEOUT_MS: 20000,
  LAVALINK_CONNECT_RETRY_DELAY_MS: 2000,
  LAVALINK_CONNECT_TIMEOUT_MS: 5000,
  LAVALINK_HEALTH_CHECK_INTERVAL_MS: 15000,
  NODE_RECONNECT_COOLDOWN_MS: 15000,
  ALL_NODES_DOWN_TIMEOUT_MS: 60000,
  VOLUME_DECREMENTER: 0.75,
  POSITION_UPDATE_INTERVAL_MS: 50,

  // === Track Cache ===
  TRACK_CACHE_TTL_MS: 60 * 60 * 1000,
  TRACK_CACHE_PRUNE_INTERVAL_MS: 10 * 60 * 1000,
  RECOVERING_GUILDS_TTL_MS: 10 * 60 * 1000,
  FAILOVER_CLEANUP_THRESHOLD_MS: 10 * 60 * 1000,

  // === State Restore ===
  POSITION_SYNC_INTERVAL_MS: 1000,
  STATE_RESTORE_RETRY_DELAY_MS: 3000,
  STATE_RESTORE_LAVALINK_MAX_RETRIES: 10,
  STATE_RESTORE_STALE_THRESHOLD_MS: 10 * 60 * 1000,
  POSITION_CAP_OFFSET_MS: 1000,

  // === Watchdog ===
  WATCHDOG_CHECK_INTERVAL_MS: 30000,
  WATCHDOG_MAX_STUCK: 3,
  WATCHDOG_MAX_RECONNECT: 3,
  WATCHDOG_RECONNECT_BACKOFF_MS: 5000,
  WATCHDOG_POST_CONNECT_WAIT_MS: 500,

  // === Load Balance ===
  LB_PENALTY_FAILED_LOAD: 50,
  LB_PENALTY_DISCONNECT: 100,
  LB_PENALTY_ERROR: 75,
  LB_PENALTY_HIGH_LATENCY: 50,
  LB_LATENCY_THRESHOLD_MS: 800,
  LB_DECAY_FACTOR: 0.5,
  LB_DECAY_INTERVAL_MS: 300000,

  // === Track Error ===
  ERROR_DEDUP_WINDOW_MS: 15000,
  ERROR_MAX_RECENT: 5,

  // === Autoplay ===
  AUTOPLAY_PLAYED_TRACKS_MAX: 100,
  AUTOPLAY_SHORT_TRACK_THRESHOLD_MS: 30000,
  AUTOPLAY_MAX_DURATION_RATIO: 0.4,

  // === Search ===
  SEARCH_CACHE_MAX_SIZE: 500,
  SEARCH_CACHE_TTL_MS: 3600000,

  // === Spotify ===
  SPOTIFY_RESOLVE_BATCH_SIZE: 5,
  SPOTIFY_CACHE_TTL_MS: 30 * 60 * 1000,
  SPOTIFY_CACHE_MAX_SIZE: 500,
  SPOTIFY_MAX_RETRIES: 2,
  SPOTIFY_RETRY_DELAY_MS: 1000,
  SPOTIFY_FETCH_TIMEOUT_MS: 20000,
  SPOTIFY_PLAYLIST_MAX_TRACKS: 500,

  // === AI ===
  AI_RESPONSE_TIMEOUT_MS: 30000,
  AI_TASK_MAX_RETRIES: 1,
  AI_MEMORY_FETCH_LIMIT: 20,

  // === Activity ===
  ACTIVITY_FLUSH_INTERVAL_MS: 30000,
  ACTIVITY_BUFFER_SIZE: 500,
  ACTIVITY_MAX_BACKOFF_MS: 300000,
  ACTIVITY_CLEANUP_DAYS: 30,

  // === Misc ===
  EMBED_SEND_DELAY_MS: 500,
  GUILD_CACHE_TTL_MS: 60000,
  ENGINE_CLEANUP_INTERVAL_MS: 300000,
};
